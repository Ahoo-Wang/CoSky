spring:
  cloud:
    govern:
      discovery:
        registry:
          weight: 8
  web:
    resources:
      static-locations: file:./dashboard/
  servlet:
    multipart:
      max-file-size: 10MB
